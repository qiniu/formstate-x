import { fromV2, toV2 } from '.'

describe('fromV2', () => {
  it('should be callable', () => {
    expect(typeof fromV2).toBe('function')
  })
})

describe('toV2', () => {
  it('should be callable', () => {
    expect(typeof toV2).toBe('function')
  })
})
